{
  "name": "nodejs",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"http-server -p 3000 -o -c-1 -a localhost ./public\" \"json-server --watch db/db.json --port 3000\""
  },
  "dependencies": {
    "concurrently": "^6.2.1",
    "http-server": "^14.0.0",
    "json-server": "^0.17.4"
  }
}
